let current_users = ["Ayesha", "Daniyal", "Sahar", "Mirha"];
let new_users = ["Danish", "Ayesha", "Sahar", "Ali", "waqas"];
current_users.forEach((new_users) => {
    if (current_users.some(element => (new_users))) {
        console.log("The person name is unavailable");
    }
    else {
        console.log("The username is available.");
    }
});
export {};
